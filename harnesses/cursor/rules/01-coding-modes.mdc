---
description: "项目工作模式检测：通过 feature-list.json 和 progress.json 判断工作模式（automated/spec-coding/vibe-coding）。当用户开始新项目、询问工作流程、或提及 feature-list / progress.json 时应用此规则。"
alwaysApply: false
---

## 第一步：检测工作模式

每次会话开始时，先执行模式检测：

```bash
pwd
ls feature-list.json progress.json 2>/dev/null
```

根据结果确定模式：

| 条件 | 模式 |
|------|------|
| `progress.json` + `feature-list.json` 同时存在 | **automated** |
| 只有 `feature-list.json` 存在 | **spec-coding** |
| 两者都不存在 | **vibe-coding** |

---

## Automated 模式

适用场景：脚本驱动，无人值守，严格约束。

### 会话开始：完整状态检查（必须按序执行）

```bash
# 1. 查看最近进展
git log --oneline -10

# 2. 读取当前进度
cat progress.json

# 3. 查看功能列表
cat feature-list.json

# 4. 验证测试可运行
<运行 progress.json 中的 test_command>
```

向用户确认：当前 session 数、已完成 / 总数、本次准备实现的功能 ID + 名称 + 验收标准。

### 选择功能

- 从 `feature-list.json` 选第一个 `status: "failing"` 的 `high` 优先级功能
- 如无 high 选 medium，如无 medium 选 low
- 一次只做一个功能

### 实现功能

1. 如果功能的 `spec_ref` 字段非空，先读取设计文档对应章节
2. 理解验收标准，先思考实现方案
3. 写测试（TDD 倾向）
4. 实现代码
5. 验证测试通过
6. 验证已有测试无回归

### 完成功能

更新 `feature-list.json` 中对应条目的 status 为 `"passing"`，更新 `progress.json`：

```json
{
  "last_updated": "<ISO 时间>",
  "current_session": "<N>",
  "completed_features": "<新数量>",
  "status": "in_progress",
  "last_completed": "<功能 ID>: <功能名称>",
  "next_steps": "<下一个功能 ID 和名称>",
  "notes": "<本次关键决策或注意事项>"
}
```

然后立即 commit：```bash
git add .
git commit -m "feat(<scope>): <功能描述> [progress: X/N features]"
```

### 会话结束

```bash
# 1. 运行完整测试套件
<test_command>

# 2. 确认无回归后更新 progress.json
# 3. commit 所有变更
```

执行 git add + git commit，message 遵循 conventional commits 格式

输出会话摘要：本次完成的功能（ID + 名称）、总进度（X/N）、下一个功能、需要用户关注的事项。

如果本次遇到多次尝试才解决的问题，或发现可复用的实现模式，运行 `/update-context` skill。

### 约束（硬性规则）

- **不删除测试**：任何情况下不删除或注释掉已有测试
- **不改 assertion**：测试失败时修复代码，不修改断言
- **一次一个功能**：完成并 commit 后再开始下一个
- **失败立即停**：更新 progress.json 记录阻塞原因，通知用户
