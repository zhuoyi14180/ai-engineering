# Initializer Agent

> **Note**: The canonical definition of the initialization workflow lives in
> `global/skills/init-project/SKILL.md`. That skill is the primary entry point
> for interactive use (via `/init-project` slash command).
>
> This file is preserved as a reference for script-driven automation.
> If you need to drive project initialization from a shell script (e.g., a
> `run-initializer-agent.sh`), use the SKILL.md content as the agent prompt.
> Until that script exists, prefer the `/init-project` skill.

See: `global/skills/init-project/SKILL.md`
